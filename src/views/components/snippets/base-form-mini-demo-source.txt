<template>
  <BaseFormMini
    ref="formRef"
    v-model="formData"
    :config="formConfig"
    :show-menu="true"
    @submit="onSubmit"
  />
</template>

<script setup>
import { reactive, ref } from 'vue'
import { BaseFormMini } from 'base-on-ui'
import 'base-on-ui/style.css'

const formRef = ref(null)
const formData = reactive({})

const formConfig = reactive({
  labelWidth: '100px',
  labelPosition: 'right',
  formItems: [
    { component: 'divider', name: 'secBase', label: '基本信息', span: 24 },
    {
      component: 'input',
      name: 'title',
      label: '标题',
      span: 12,
      value: '',
      rules: [{ required: true, message: '请输入标题', trigger: 'blur' }],
      options: { placeholder: '请输入标题', clearable: true },
    },
    {
      component: 'select',
      name: 'type',
      label: '类型',
      span: 12,
      value: '1',
      options: {
        placeholder: '请选择',
        items: [
          { label: '普通', value: '1' },
          { label: '高级', value: '2' },
        ],
      },
    },
    {
      component: 'input',
      name: 'remark',
      label: '高级备注',
      span: 24,
      value: '',
      hideHandle: '$.type !== "2"',
      options: { placeholder: '仅类型为「高级」时显示' },
    },
    { component: 'divider', name: 'secExtra', label: '其它', span: 24 },
    {
      component: 'switch',
      name: 'enabled',
      label: '启用',
      span: 12,
      value: true,
      options: {},
    },
  ],
})

function onSubmit(form) {
  console.log('submit', form)
}
</script>
